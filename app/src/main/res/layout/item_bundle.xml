<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="4dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <ImageView
            android:id="@+id/bundle_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@android:drawable/ic_menu_gallery"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            android:contentDescription="Icon" />

        <TextView
            android:id="@+id/bundle_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            tools:text="Название бандла"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@+id/bundle_action_button"
            app:layout_constraintStart_toEndOf="@+id/bundle_icon"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/bundle_type"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="4dp"
            android:background="#E0E0E0"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:textColor="@color/design_default_color_on_secondary"
            android:textSize="10sp"
            app:layout_constraintStart_toEndOf="@+id/bundle_icon"
            app:layout_constraintTop_toBottomOf="@+id/bundle_title"
            tools:text="LATEST" />

        <TextView
            android:id="@+id/bundle_description"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            tools:text="Описание..."
            android:textColor="#757575"
            android:textSize="12sp"
            app:layout_constraintEnd_toStartOf="@+id/bundle_action_button"
            app:layout_constraintStart_toEndOf="@+id/bundle_icon"
            app:layout_constraintTop_toBottomOf="@+id/bundle_type" />

        <ProgressBar
            android:id="@+id/bundle_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:max="100"
            android:visibility="gone"
            tools:progress="50"
            tools:visibility="visible"
            app:layout_constraintEnd_toStartOf="@+id/bundle_action_button"
            app:layout_constraintStart_toEndOf="@+id/bundle_icon"
            app:layout_constraintTop_toBottomOf="@+id/bundle_description" />

        <TextView
            android:id="@+id/bundle_status_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textSize="10sp"
            android:textColor="#546E7A"
            android:visibility="gone"
            tools:text="Загрузка: scp-173... (5/10)"
            tools:visibility="visible"
            app:layout_constraintEnd_toStartOf="@+id/bundle_action_button"
            app:layout_constraintStart_toEndOf="@+id/bundle_icon"
            app:layout_constraintTop_toBottomOf="@+id/bundle_progress_bar"
            app:layout_constraintBottom_toBottomOf="parent" />

        <ImageButton
            android:id="@+id/bundle_action_button"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/stat_notify_sync_noanim"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>